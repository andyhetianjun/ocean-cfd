# Legacy: olaFlow Monopile Case (Stage 3, June 2026)

Config-only snapshot of an early wave-monopile case built with **olaFlow**
(not waves2Foam) before switching solvers. Run output (decomposed timesteps,
~48GB) was deleted; these are the case definition files only, kept for
reference on cylinder/monopile geometry and BC setup approach.

Not part of active Task 1-3 work.
